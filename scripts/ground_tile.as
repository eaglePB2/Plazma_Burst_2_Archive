package
{
   import flash.display.BitmapData;
   
   public dynamic class ground_tile extends BitmapData
   {
       
      
      public function ground_tile(w:Number, h:Number)
      {
         super(w,h);
      }
   }
}
