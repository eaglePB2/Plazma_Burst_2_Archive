package
{
   import flash.display.BitmapData;
   
   public dynamic class red extends BitmapData
   {
       
      
      public function red(w:Number, h:Number)
      {
         super(w,h);
      }
   }
}
