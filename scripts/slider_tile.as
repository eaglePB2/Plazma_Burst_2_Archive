package
{
   import flash.display.BitmapData;
   
   public dynamic class slider_tile extends BitmapData
   {
       
      
      public function slider_tile(w:Number, h:Number)
      {
         super(w,h);
      }
   }
}
